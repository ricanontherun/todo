module.exports = {
    TODOS: {
        STATES: {
            OPEN: 0,
            COMPLETE: 1
        }
    },
    APP: {
        STATES: {
            // Opened, no tasks added
            UNUSED: 0,

            // A task has been added.
            USED: 1
        }
    }
}