import { AppRegistry } from 'react-native';

// Pull in our app component.
import App from './src/components/App';

// Register the component under a name, myfirstapp in this case.
AppRegistry.registerComponent('myfirstapp', () => App);
